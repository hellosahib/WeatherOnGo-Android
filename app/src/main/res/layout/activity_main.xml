<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:tools="http://schemas.android.com/tools">
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@mipmap/ic_launcher"
        android:scaleType="centerCrop"
        android:id="@+id/iv_main_backgroundImage"
        android:contentDescription="Location Image"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="Its Cold Outside Go Home"
        android:textSize="36sp"
        android:padding="16dp"
        android:textColor="@android:color/white"
        android:id="@+id/tv_main_weatherStatus"/>
    <View
        android:layout_width="56dp"
        android:layout_height="16dp"
        android:background="@android:color/white"
        android:id="@+id/v_main_linebreak"
        android:layout_below="@id/tv_main_weatherStatus"
        android:layout_margin="16dp"
        />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="San Fransico , USA"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:layout_below="@id/v_main_linebreak"
        android:padding="8dp"
        android:id="@+id/tv_main_location"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:text="12 C"
        android:textColor="@android:color/white"
        android:textSize="24sp"
        android:layout_below="@id/tv_main_location"
        android:padding="8dp"
        android:id="@+id/tv_main_temprature"/>

</RelativeLayout>